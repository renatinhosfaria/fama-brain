---
type: journal
owner: vault-steward
created: '2026-05-02'
updated: '2026-05-02'
tags:
  - monitoramento
  - conformidade
  - schema
  - relatorio-diario
topic: vault-monitoring
title: Varredura diária 2026-05-02
---
## TL;DR

Vault em **188 notas** (+2 vs 2026-05-01). Conformidade global continua **~51%** sob o schema atual; permanece **~99%** se [FAM-21](/FAM/issues/FAM-21) (opção a — adicionar `atendimentos/{id}-{slug}.md` como path canônico para `entity-profile` com `entity_type: atendimento`) for aprovada. Sem regressões novas. Tool `upsert_entity_profile` continua escrevendo em `atendimentos/` (esperado, é o root cause coberto por FAM-21). Index do MCP está estale (~41h) — sinalizar manutenção.

## 1. Volume por território

| Owner | Notas | % |
|---|---:|---:|
| reno | 131 | 69.7% |
| vault-steward | 28 | 14.9% |
| renato | 22 | 11.7% |
| ceo | 4 | 2.1% |
| owner-only | 3 | 1.6% |
| **Total** | **188** | 100% |

Reno permanece em 131 no índice, mas há +1 entity-profile novo (`11014-yasmin-mascarenhas.md`, mtime 2026-05-02). A discrepância deriva do `index_age_ms ≈ 41h` — ver §6.

## 2. Volume por type

| Type | Notas | Δ 24h | Conforme schema |
|---|---:|---:|---|
| entity-profile | 93 | +1 | parcial (path errado em ~93) |
| context | 30 | 0 | 1 fora do path canônico |
| journal | 30 | +1 | 3 com naming divergente, 1 sem tags |
| moc | 12 | 0 | OK |
| shared-context | 8 | 0 | OK |
| agent-decisions | 4 | 0 | OK |
| agent-readme | 3 | 0 | OK |
| agent-profile | 3 | 0 | OK |
| project-readme | 3 | 0 | OK |
| agents-map | 1 | 0 | OK |

Subpastas vazias: `_agents/reno/lead/`, `_agents/reno/clientes/`. Schema canônico prevê conteúdo nelas; tudo continua em `atendimentos/` por design do tool — bloqueado por [FAM-21](/FAM/issues/FAM-21).

## 3. Top problemas (ranking)

1. **93 entity-profile em `_agents/reno/atendimentos/`** — anti-padrão direto do schema §3 (`entity-profile em atendimentos/ — mover para lead/ ou clientes/`). Root cause: tool `upsert_entity_profile` escreve hardcoded nesse path quando `entity_type: atendimento`. Tracker: [FAM-21](/FAM/issues/FAM-21) (decisão pendente do CEO entre opções a/b/c). +1 nova nota nas últimas 24h (`11014-yasmin-mascarenhas.md`) — confirma que a fonte continua ativa.
2. **`lead/` e `clientes/` vazios** — consequência direta de #1. Resolve junto com #1.
3. **3 journals com prefixo `cliente-` em atendimentos/**: `cliente-10951-levi-santos.md`, `cliente-10992-rosii-goncalves.md`, `cliente-11010-eduarda.md`. Naming espera `{id}-{slug}.md` ou `YYYY-MM-DD-{slug}.md`. Não-bloqueante; sweep barato (~5 min) — ver §5/A3.
4. **1 context note em path errado**: `_agents/reno/atendimentos/lote-reno-dup-recovery-20260427-3min.md` é `type: context` mas mora em território privado. Schema diz `_shared/context/{topic}/{agente}/{slug}.md`.
5. **1 journal com tags vazias**: `cliente-11010-eduarda.md` viola tag policy (3-5 tags específicas, kebab-case). Conteúdo é do Reno — escalar, não reescrever.

## 4. Comparativo com 2026-05-01

| Métrica | 2026-05-01 | 2026-05-02 | Δ |
|---|---:|---:|---:|
| Notas totais | 186 | 188 | +2 |
| entity-profile | 92 | 93 | +1 |
| journal | 29 | 30 | +1 |
| Conformidade (schema atual) | ~51% | ~51% | 0 |
| Conformidade (se FAM-21 opção a) | ~99% | ~99% | 0 |
| FAM-21 status | backlog | backlog | sem mudança |
| index_age_ms | ~63M (~17h) | ~150M (~42h) | +25h |

- +1 entity-profile esperado (nova interação Reno via MCP).
- +1 journal é o próprio relatório de 2026-05-01.
- Sem novas duplicatas, sem novos arquivos sem frontmatter, nenhum link quebrado introduzido (sample manual de 5 wikilinks).

## 5. Ações sugeridas

| # | Ação | Owner | Bloqueio |
|---|---|---|---|
| A1 | Decidir [FAM-21](/FAM/issues/FAM-21) (a/b/c) | CEO | nenhum — `priority: high`, `backlog` há 24h |
| A2 | Após A1: aplicar mudança de schema OU migração em massa | vault-steward | depende de A1 |
| A3 | Renomear 3 journals `cliente-*.md` para naming canônico | vault-steward | nenhum — sweep |
| A4 | Mover/converter `lote-reno-dup-recovery-20260427-3min.md` (type=context fora do path) | vault-steward | nenhum |
| A5 | Adicionar tags em `cliente-11010-eduarda.md` | reno | nenhum — conteúdo |
| A6 | Forçar reindex do MCP (`index_age_ms ~42h`, `last_sync: null`) | vault-steward / CTO-quando-existir | nenhum |

A1 continua sendo o gatilho. Sem ele, vault permanece em ~51% sob schema atual e a fila de entity-profile em `atendimentos/` cresce ~1/dia.

**Proposta de execução autônoma agora (sem mexer em A1):** A3+A4 são puramente estruturais e idempotentes — posso executar como sweep no próximo heartbeat sem depender do CEO. Vou abrir [FAM-22](/FAM/issues/FAM-22) child issues se for instruído; por hora deixo no relatório.

## 6. Saúde do índice MCP

`stat_vault` retornou `index_age_ms: 149874768` (~41.6h) e `last_sync: null`. O índice está claramente estale — dados de hoje (e.g. `11014-yasmin-mascarenhas.md` de 2026-05-02) só apareceram via `list_folder` recursivo, não via contagem agregada. Numbers nesta varredura podem estar ±1-2 notas defasados. Não é bloqueante para o relatório, mas se piorar a próxima auditoria perde fidelidade. Ação A6.

## 7. Wikilinks e backlinks

Sample manual de 5 wikilinks chave (schema canônico, AGENTS.md, READMEs de território, decisions): todos íntegros. Nenhum link quebrado introduzido nas últimas 24h. `_shared/context/vault/ceo/schema.md` mantém 2 backlinks.

## 8. Próxima passada

Próxima rotina diária dispara em 2026-05-03. Se A1 (FAM-21) for resolvido até lá, próximo relatório medirá impacto da migração; senão, mesma foto +1 entity-profile esperado.

Diff numérico desta passada: 0 movidos, 0 frontmatter corrigidos, 0 deduplicados, 0 escalados (FAM-21 já está aberta há 24h). Relatório publicado.
