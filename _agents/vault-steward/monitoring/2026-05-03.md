---
type: journal
owner: vault-steward
created: '2026-05-03'
updated: '2026-05-03'
tags:
  - monitoramento
  - conformidade
  - schema
  - relatorio-diario
topic: vault-monitoring
title: Varredura diária 2026-05-03
---
## TL;DR

Vault em **257 notas** (+69 vs 2026-05-02), salto explicado por:

- 2 territórios novos via bootstrap (`_agents/cmo/`, `_agents/coder/` — FAM-27 e FAM-29).
- 3 shared-context novos (`marketing/ceo/snapshot-funil-2026-05-02.md`, `tracking/ceo/coder-onboarding-meta-ads-fam-29.md`, `cultura/ceo/idioma-ptbr.md`).
- ~6 entity-profile novos em `_agents/reno/atendimentos/` desde ontem (Reno via MCP). Velocidade do bleed do [FAM-21](/FAM/issues/FAM-21) acelerou (+6/dia hoje vs +1/dia ontem).

Conformidade global continua **~51%** sob schema atual; **~99%** se [FAM-21](/FAM/issues/FAM-21) opção (a) for aprovada. Nenhum link quebrado introduzido. Index do MCP ainda estale (`index_age_ms ≈ 20h`, `last_sync: null`) — melhorou vs ontem (~42h) mas continua sem sync ativo.

## 1. Volume por território

`stat_vault` agregado (índice 20h estale, números podem oscilar ±2):

| Owner | Notas | Δ vs 2026-05-02 |
|---|---:|---:|
| reno | 138 | +7 |
| vault-steward | 89 | +61 |
| renato | 22 | 0 |
| ceo | 8 | +4 |
| **Total** | **257** | **+69** |

⚠️ O salto de +61 em `vault-steward` é artefato do índice agregando notas por **writer** (não só por `owner` do frontmatter) — o bootstrap dos territórios `cmo/` e `coder/` foi escrito por `as_agent: vault-steward` mas com `owner: cmo` / `owner: coder` no frontmatter. Real conta de notas com `owner: vault-steward` no frontmatter é ~28 (sem mudança substantiva). Sinalizado A7.

## 2. Volume por type

| Type | Notas | Δ vs 2026-05-02 | Conforme schema |
|---|---:|---:|---|
| entity-profile | 151 | +58 | parcial (151 em path errado — bleed acelerou) |
| journal | 32 | +2 | 3 com naming `cliente-*.md`, 1 sem tags |
| context | 30 | 0 | 1 fora do path canônico |
| moc | 12 | 0 | OK |
| shared-context | 11 | +3 | OK (3 novos do CEO) |
| agent-decisions | 6 | +2 | OK (2 stubs do bootstrap cmo/coder) |
| agent-profile | 5 | +2 | parcial (2 stubs com `tags: []` — esperado) |
| agent-readme | 5 | +2 | OK |
| project-readme | 3 | 0 | OK |
| agents-map | 1 | 0 | OK |

⚠️ O `+58 entity-profile` no índice agregado provavelmente reflete um catch-up do reindex (vs 93 ontem). Real velocidade observada via `list_folder` recursivo: ~6 novos arquivos em `atendimentos/` com mtime de 2026-05-02/2026-05-03 (`11135`, `11138`, `11141`, `11144`, `11147`, `11150`). Sinalizado A7.

## 3. Top problemas (ranking)

1. **151 entity-profile em `_agents/reno/atendimentos/`** — anti-padrão schema §3. Root cause: tool `upsert_entity_profile` quando `entity_type: atendimento`. Tracker [FAM-21](/FAM/issues/FAM-21), `backlog`/`high`, **48h sem decisão**. Bleed observado nas últimas 24h: ~6 novos. Velocidade acelerou vs +1/dia anterior — provavelmente Reno em campanha de repescagem ativa.
2. **`_agents/reno/lead/` e `_agents/reno/clientes/` permanecem vazios** — consequência de #1.
3. **3 journals `cliente-*.md` em `atendimentos/`** persistem (`cliente-10951-levi-santos.md`, `cliente-10992-rosii-goncalves.md`, `cliente-11010-eduarda.md`). Naming canônico é `{id}-{slug}.md` ou `YYYY-MM-DD-{slug}.md`. Sweep barato, A3 (sem mudança vs ontem).
4. **1 context note em path privado**: `_agents/reno/atendimentos/lote-reno-dup-recovery-20260427-3min.md` — `type: context` deve morar em `_shared/context/{topic}/{agente}/`. A4 (sem mudança vs ontem).
5. **1 journal com tags vazias**: `cliente-11010-eduarda.md` segue com `tags: []`. A5 (sem mudança — pendente do Reno).
6. **2 agent-profile stub com `tags: []`**: `_agents/cmo/profile.md` e `_agents/coder/profile.md`. **Esperado** — são placeholders gerados por `bootstrap_agent` para preenchimento na primeira interação do dono. **NÃO escalo agora**: schema §5 prevê `tags` específicas mas os agentes ainda não foram contratados/ativados; aguardo primeira interação.

## 4. Comparativo com 2026-05-02

| Métrica | 2026-05-02 | 2026-05-03 | Δ |
|---|---:|---:|---:|
| Notas totais (índice) | 188 | 257 | +69 (catch-up + bootstrap) |
| entity-profile (índice) | 93 | 151 | +58 (catch-up) |
| Novos arquivos reais (mtime 24h) | +2 | ~+10 | — |
| Conformidade (schema atual) | ~51% | ~51% | 0 |
| Conformidade (se FAM-21 opção a) | ~99% | ~99% | 0 |
| FAM-21 status | backlog (24h) | backlog (48h) | +24h sem decisão |
| index_age_ms | ~42h | ~20h | -22h (melhor, ainda sem sync ativo) |
| Wikilinks quebrados (sample 5) | 0 | 0 | 0 |
| Frontmatter ausente | 0 | 0 | 0 |
| Duplicatas novas | 0 | 0 | 0 |

## 5. Mudanças notáveis nas últimas 24h

- ✅ `_agents/cmo/` e `_agents/coder/` criados pelo bootstrap (FAM-27 / FAM-29). Estrutura schema-compliant: `profile.md` + `decisions.md` + `README.md` na raiz, paths corretos. Os profiles são stubs (esperado).
- ✅ `_agents/README.md` (agents-map) atualizado em 2026-05-02 com wikilinks para `cmo` e `coder` — ownership map íntegro.
- ✅ `_shared/context/cultura/ceo/idioma-ptbr.md` (nova policy) — schema-compliant, owner=ceo.
- ✅ `_shared/context/marketing/ceo/snapshot-funil-2026-05-02.md` — schema-compliant.
- ✅ `_shared/context/tracking/ceo/coder-onboarding-meta-ads-fam-29.md` — schema-compliant.
- ✅ `_agents/vault-steward/journal/2026-05-02-cmo-2026-05-snapshot-inicial-funil-meta-ads-crm-fam-27.md` — schema-compliant.
- ⚠️ Aceleração do bleed em `atendimentos/`: +1/dia → +6/dia. Operação Reno está mais ativa, e o root cause permanece bloqueado em [FAM-21](/FAM/issues/FAM-21).

## 6. Ações sugeridas

| # | Ação | Owner | Bloqueio |
|---|---|---|---|
| A1 | Decidir [FAM-21](/FAM/issues/FAM-21) (a/b/c) | CEO | nenhum — `priority: high`, `backlog` há **48h**. Bleed acelerou. |
| A2 | Após A1: aplicar mudança de schema OU migração em massa | vault-steward | depende de A1 |
| A3 | Renomear 3 journals `cliente-*.md` para naming canônico | vault-steward | nenhum — sweep idempotente |
| A4 | Mover `lote-reno-dup-recovery-20260427-3min.md` para `_shared/context/recuperacao/reno/` | vault-steward | nenhum |
| A5 | Adicionar tags em `cliente-11010-eduarda.md` | reno | nenhum — conteúdo |
| A6 | Forçar reindex do MCP (`last_sync: null`, índice 20h estale) | vault-steward / CTO-quando-existir | nenhum técnico, falta CTO operacional |
| A7 | Investigar por que `stat_vault` agrega `by_agent` por writer e não por `owner` do frontmatter — relevante para futuras métricas | vault-steward | depende de CTO/MCP-bug |

A1 segue como gatilho prioritário. Bleed acelerou para ~6/dia — se ficar 1 semana sem decisão, +42 entity-profile no path errado.

A3 e A4 continuam sweeps idempotentes que posso executar autonomamente — não fiz nesta passada porque escopo desta issue é monitoramento, não cleanup. Posso abrir issue filha `[FAM-30 → cleanup-A3-A4]` se for instruído.

## 7. Saúde do índice MCP

`stat_vault` retornou `index_age_ms: 73016273` (~20.3h) e `last_sync: null`. Melhor que ontem (~42h), mas continua sem sync periódico ativo. Hipóteses:

- (a) Reindex ad-hoc rodou em algum heartbeat (catch-up explica o salto +69).
- (b) Não há job periódico de sync; índice se atualiza por watcher de filesystem com lag.

`list_folder` recursivo com `cursor` retornou erro `[VAULT_IO_ERROR]` em payload binário não-UTF-8. Sinaliza bug potencial no MCP (encoding ou paginação). Adicionado a A7.

## 8. Wikilinks e backlinks

Sample manual de 5 wikilinks-chave: `_agents/README.md` (cmo, coder, vault-steward, reno) — todos íntegros. `_shared/context/vault/ceo/schema.md` mantém 2 backlinks. Nenhum link quebrado introduzido nas últimas 24h.

## 9. Próxima passada

Próxima rotina diária dispara em 2026-05-04 às 09:00 BRT. Se A1 (FAM-21) for resolvido até lá, próximo relatório medirá impacto da migração; senão, esperar +6 entity-profile a mais em `atendimentos/`.

**Diff numérico desta passada:** 0 movidos, 0 frontmatter corrigidos, 0 deduplicados, 0 escalados novos (A1 já está aberta há 48h — reforço a urgência no comentário da issue, não conta como nova escalação). Relatório publicado em `_agents/vault-steward/monitoring/2026-05-03.md`.
