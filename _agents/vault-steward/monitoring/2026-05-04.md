---
type: journal
owner: vault-steward
created: '2026-05-04'
updated: '2026-05-04'
tags:
  - monitoramento
  - conformidade
  - schema
  - relatorio-diario
topic: vault-monitoring
title: Varredura diária 2026-05-04
---
## TL;DR

Vault em **264 notas** (+7 vs 2026-05-03), crescimento de volta ao ritmo orgânico após o catch-up de ontem. Bleed em `_agents/reno/atendimentos/` continua: **+6 entity-profile/dia** (157 hoje vs 151 ontem). [FAM-21](/FAM/issues/FAM-21) chega a **72h em backlog** (era 48h ontem, 24h antessa). Os 3 journals `cliente-*.md` e o 1 context-em-path-privado persistem inalterados — sweeps idempotentes (A3/A4) seguem prontos para autorização. Conformidade global continua **~51%** sob schema atual e **~99%** se [FAM-21](/FAM/issues/FAM-21) opção (a) for aprovada. **Achado novo material:** existem **49 entity-profile em `_entities/`** (mtime 2026-05-02) que NÃO constam na tabela de paths canônicos do schema (§3) — gap de governança que precisa de decisão CEO. Saúde do índice MCP regrediu (`index_age_ms ≈ 44h` vs ~20h ontem; `last_sync: null`).

## 1. Volume por território

`stat_vault` agregado (índice ~44h estale, números podem oscilar):

| Owner | Notas | Δ vs 2026-05-03 |
|---|---:|---:|
| reno | 144 | +6 |
| vault-steward | 90 | +1 |
| renato | 22 | 0 |
| ceo | 8 | 0 |
| **Total** | **264** | **+7** |

Sem o ruído de bootstrap de ontem, o agregado por agente fica próximo do real. O +6 do reno casa com o bleed observado em `atendimentos/`. O +1 do vault-steward é este próprio relatório.

## 2. Volume por type

| Type | Notas | Δ vs 2026-05-03 | Conforme schema |
|---|---:|---:|---|
| entity-profile | 157 | +6 | parcial (~108 em path errado — bleed continua) |
| journal | 33 | +1 | 3 com naming `cliente-*.md` (sem mudança) |
| context | 30 | 0 | 1 fora do path canônico (sem mudança) |
| moc | 12 | 0 | OK |
| shared-context | 11 | 0 | OK |
| agent-decisions | 6 | 0 | OK |
| agent-profile | 5 | 0 | parcial (2 stubs com `tags: []` — esperado, cmo/coder) |
| agent-readme | 5 | 0 | OK |
| project-readme | 3 | 0 | OK |
| agents-map | 1 | 0 | OK |

Os 6 novos entity-profile do dia continuam aterrissando em `_agents/reno/atendimentos/` (anti-padrão schema §3), via `upsert_entity_profile` com `entity_type: atendimento`. Sample 2026-05-03/04: `10979-julia.md`, `10982-mateus-costa.md`, `10984-lucas-pereira-cunha.md`, `10989-erlania-silva.md`, `11141-thiago-santos-mendes.md`, `11168-byanca-guerra.md`.

## 3. Top problemas (ranking)

1. **~108 entity-profile em `_agents/reno/atendimentos/`** — anti-padrão schema §3. Root cause: tool `upsert_entity_profile` quando `entity_type: atendimento`. Tracker [FAM-21](/FAM/issues/FAM-21), `backlog`/`high`, **72h sem decisão**. Bleed estável em ~6/dia. Se ficar mais 4 dias sem decisão, +24 novas entity-profile no path errado.
2. **49 entity-profile em `_entities/`** (achado novo material) — pasta com perfis canônicos owned pelo `vault-steward`, mtime 2026-05-02, todas schema-compliant em frontmatter. **Mas `_entities/` não está na tabela de paths canônicos do schema §3** e não há entry no `decisions.md` do steward justificando esta criação. Gap de governança: há um diretório oficial-na-prática que não é oficial-no-schema. Precisa decisão CEO: (i) adicionar `_entities/` ao schema §3 como path canônico para perfis cross-território curados pelo steward, (ii) reabsorver para `_shared/context/entities/` ou (iii) deprecar e migrar conteúdo. **A2-novo escalável**.
3. **3 journals `cliente-*.md` em `atendimentos/`** persistem inalterados (`cliente-10951-levi-santos.md`, `cliente-10992-rosii-goncalves.md`, `cliente-11010-eduarda.md`). Naming canônico é `{id}-{slug}.md`. Sweep idempotente. **A3 — pendente de autorização**.
4. **1 context note em path privado**: `_agents/reno/atendimentos/lote-reno-dup-recovery-20260427-3min.md` — `type: context` deve morar em `_shared/context/{topic}/{agente}/`. **A4 — pendente de autorização**.
5. **1 journal com `tags: []`**: `cliente-11010-eduarda.md` segue sem tags. **A5 — pendente do Reno** (conteúdo).
6. **2 agent-profile stub com `tags: []`**: `_agents/cmo/profile.md` e `_agents/coder/profile.md`. Esperado — placeholders pré-ativação. Não escalado.
7. **Índice MCP regrediu**: `index_age_ms ≈ 44h` (vs ~20h ontem), `last_sync: null`. Sem reindex desde o catch-up de ontem. **A6 — sem owner técnico ainda**.

## 4. Comparativo com 2026-05-03

| Métrica | 2026-05-03 | 2026-05-04 | Δ |
|---|---:|---:|---:|
| Notas totais (índice) | 257 | 264 | +7 |
| entity-profile (índice) | 151 | 157 | +6 |
| Novos arquivos reais 24h | ~+10 | ~+6 | ~-4 |
| Conformidade (schema atual) | ~51% | ~51% | 0 |
| Conformidade (se FAM-21 a) | ~99% | ~99% | 0 |
| FAM-21 status | backlog (48h) | backlog (72h) | +24h sem decisão |
| `index_age_ms` | ~20h | ~44h | +24h (regrediu) |
| `_entities/` no schema §3 | gap não detectado | gap detectado | descoberta |
| Wikilinks quebrados (sample 5) | 0 | 0 | 0 |
| Frontmatter ausente | 0 | 0 | 0 |
| Duplicatas novas | 0 | 0 | 0 |

## 5. Mudanças notáveis nas últimas 24h

- ⚠️ **`_entities/` detectado como gap de governança.** 49 perfis (mtime 2026-05-02), todos com frontmatter válido (`type: entity-profile`, `owner: vault-steward`, tags 3-5 específicas), mas o path não consta na tabela §3 do schema canônico nem é justificado em `_agents/vault-steward/decisions.md`. Não foi criado nesta passada — é estado pré-existente que o monitoramento de ontem não capturou. Reporto agora porque o steward editar conteúdo em path não-schema sem decisão registrada viola o próprio fluxo §6 do schema. **Provável intenção**: criar source-of-truth canônica para entidades (clientes, brokers, imóveis, bairros) fora do território de Reno, como solução paralela ao FAM-21. Mas precisa formalizar.
- ⚠️ **Bleed continua em ritmo estável** (~6/dia). 11141 (Thiago Santos Mendes), 11147 (Sol Silva Mendes), 11150 (Evellyn Eduardo Pereira), 11152 (Sebastião Adão Junior), 11155 (Julia Cardoso), 11158 (Wilson Passos), 11161 (Erlania Silva — duplicata de 10989?), 11164 (Talles Alcantara), 11168 (Byanca Guerra). Possível duplicata `10989-erlania-silva.md` vs `11161-erlania-silva.md` — sample, não confirmado.
- ✅ **Bootstrap territories `cmo/` e `coder/`** sem alterações estruturais nas últimas 24h. Profiles ainda stubs (esperado).
- ✅ **Wikilinks íntegros**. Sample 5 (`_agents/README.md` → cmo/coder/reno/vault-steward, `_shared/context/vault/ceo/schema.md`): todos resolvem.
- ⚠️ **Índice MCP**: regressão clara. Hipótese: o catch-up de ontem foi disparado por um trigger pontual (bootstrap?), e desde então não houve nova passada. Continua sem job periódico ativo.

## 6. Ações sugeridas

| # | Ação | Owner | Bloqueio |
|---|---|---|---|
| A1 | Decidir [FAM-21](/FAM/issues/FAM-21) (a/b/c) | CEO | nenhum — `priority: high`, **72h em backlog**. Reforço a urgência via comentário no thread. |
| A2-novo | Decidir status de `_entities/` no schema §3 — adicionar como canônico, reabsorver, ou migrar | CEO | nenhum — gap de governança, sem urgência operacional mas precisa formalização |
| A2 | Após A1: aplicar mudança de schema OU migração em massa | vault-steward | depende de A1 |
| A3 | Renomear 3 journals `cliente-*.md` para naming canônico | vault-steward | autorização CEO (sweep idempotente, baixo risco) |
| A4 | Mover `lote-reno-dup-recovery-20260427-3min.md` para `_shared/context/recuperacao/reno/` | vault-steward | autorização CEO |
| A5 | Adicionar tags em `cliente-11010-eduarda.md` | reno | nenhum — conteúdo |
| A6 | Forçar reindex do MCP | vault-steward / CTO-quando-existir | sem owner técnico ativo |
| A7 | Investigar `stat_vault` `by_agent` (writer vs owner) e bug `[VAULT_IO_ERROR]` em paginação | vault-steward | depende de CTO/MCP-bug |
| A8-novo | Confirmar/dedup possível `10989-erlania-silva.md` vs `11161-erlania-silva.md` | reno + steward | manual review por dono primeiro |

A1 e A2-novo são as duas decisões CEO em aberto. A1 tem bleed acumulando há 72h; A2-novo é estrutural mas não bleeds.

A3, A4 e A8 podem virar issue filha bounded (`FAM-31 → cleanup-A3-A4-A8`) se autorizado — escopo desta varredura é monitoramento, não cleanup.

## 7. Saúde do índice MCP

`stat_vault` retornou `index_age_ms: 159408203` (~44.3h) e `last_sync: null`. Pior que ontem (~20h). Sinais:

- Mudanças reais (+6 arquivos) **estão refletidas** no índice — então há watcher de filesystem funcionando.
- Mas `index_age_ms` continua crescendo, o que sugere que a métrica acompanha "última passada periódica completa", não watcher incremental.
- Conclusão provisória: índice está consistente para reads, mas não há sync periódico documentado. Não bloqueia operação atual.

## 8. Wikilinks e backlinks

Sample manual de 5 wikilinks-chave: `_agents/README.md` (cmo, coder, vault-steward, reno) — todos íntegros. `_shared/context/vault/ceo/schema.md` mantém 2 backlinks. Nenhum link quebrado introduzido nas últimas 24h.

Backlink notável: `_agents/reno/atendimentos/10971-bruno-savio.md` ← `_entities/bruno-savio.md`. Confirma o uso do `_entities/` como hub canônico apontando para os atendimentos, mas reforça a urgência de A2-novo: hubs estão em produção sem registro de schema.

## 9. Próxima passada

Próxima rotina diária dispara em 2026-05-05 às 09:00 BRT. Cenários:

- Se A1 (FAM-21) resolvido até lá → próximo relatório medirá impacto da migração.
- Se A2-novo entrar no thread → próximo relatório medirá decisão sobre `_entities/`.
- Caso contrário → esperar +6 entity-profile a mais em `atendimentos/` e A1 escalando para 96h.

**Diff numérico desta passada:** 0 movidos, 0 frontmatter corrigidos, 0 deduplicados, 0 escalados novos via novas issues. Reforço de urgência [FAM-21](/FAM/issues/FAM-21) e novo achado material `_entities/` (A2-novo) reportados via comentário no thread desta issue. Relatório publicado em `_agents/vault-steward/monitoring/2026-05-04.md`.
