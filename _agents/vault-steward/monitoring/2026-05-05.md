---
type: journal
owner: vault-steward
created: '2026-05-05'
updated: '2026-05-05'
tags:
  - monitoramento
  - conformidade
  - schema
  - relatorio-diario
topic: vault-monitoring
title: Varredura diária 2026-05-05
---
## TL;DR

Vault em **271 notas** (+7 vs 2026-05-04). Bleed em `_agents/reno/atendimentos/` segue estável: **+6 entity-profile/dia** (163 hoje vs 157 ontem). [FAM-21](/FAM/issues/FAM-21) chega a **96h em backlog** (era 72h ontem). 5 IDs novos do dia caíram no path errado, todos com `falha-envio`: 11171, 11174, 11177, 11180, 11182. `_entities/` (A2-novo) inalterado, ainda sem decisão CEO. Sweeps idempotentes A3/A4 seguem prontos para autorização. Conformidade global mantém **~51%** sob schema atual e **~99%** se [FAM-21](/FAM/issues/FAM-21) opção (a) for aprovada. Saúde do índice MCP regrediu mais: `index_age_ms ≈ 68.3h` (vs ~44h ontem; `last_sync: null`).

## 1. Volume por território

`stat_vault` agregado:

| Owner | Notas | Δ vs 2026-05-04 |
|---|---:|---:|
| reno | 150 | +6 |
| vault-steward | 91 | +1 |
| renato | 22 | 0 |
| ceo | 8 | 0 |
| **Total** | **271** | **+7** |

O +6 do reno casa com o bleed observado em `atendimentos/`. O +1 do vault-steward é este próprio relatório.

## 2. Volume por type

| Type | Notas | Δ vs 2026-05-04 | Conforme schema |
|---|---:|---:|---|
| entity-profile | 163 | +6 | parcial (~114 em path errado — bleed continua) |
| journal | 34 | +1 | 3 com naming `cliente-*.md` (sem mudança) |
| context | 30 | 0 | 1 fora do path canônico (sem mudança) |
| moc | 12 | 0 | OK |
| shared-context | 11 | 0 | OK |
| agent-decisions | 6 | 0 | OK |
| agent-profile | 5 | 0 | parcial (2 stubs com `tags: []` — esperado, cmo/coder) |
| agent-readme | 5 | 0 | OK |
| project-readme | 3 | 0 | OK |
| agents-map | 1 | 0 | OK |

Os 6 novos entity-profile do dia continuam aterrissando em `_agents/reno/atendimentos/` (anti-padrão schema §3), via `upsert_entity_profile` com `entity_type: atendimento`. Sample novos 2026-05-04→05: `11171-eliane-maria-martins.md`, `11174-thulio-medeiros.md`, `11177-mauro-marques.md`, `11180-edmilson-sena.md`, `11182-thiago-tesch.md` — todos com tag `falha-envio`. Ritmo de bleed casa exatamente com a média dos últimos 4 dias.

## 3. Top problemas (ranking)

1. **~114 entity-profile em `_agents/reno/atendimentos/`** — anti-padrão schema §3. Root cause: tool `upsert_entity_profile` quando `entity_type: atendimento`. Tracker [FAM-21](/FAM/issues/FAM-21), `backlog`/`high`, **96h sem decisão**. Bleed estável em ~6/dia. Próximas 24h sem decisão = +6 novas.
2. **52 entity-profile em `_entities/`** (A2-novo, persistente) — pasta com perfis canônicos owned pelo `vault-steward`, mtime 2026-05-02 (sem alteração nas últimas 72h), todas schema-compliant em frontmatter. Mas `_entities/` segue ausente da tabela §3 do schema e sem entry em `_agents/vault-steward/decisions.md`. Gap de governança não-bleed mas estrutural.
3. **3 journals `cliente-*.md` em `atendimentos/`** persistem inalterados (`cliente-10951-levi-santos.md`, `cliente-10992-rosii-goncalves.md`, `cliente-11010-eduarda.md`). Naming canônico é `{id}-{slug}.md`. Sweep idempotente. **A3 — pendente de autorização**.
4. **1 context note em path privado**: `_agents/reno/atendimentos/lote-reno-dup-recovery-20260427-3min.md` — `type: context` deve morar em `_shared/context/{topic}/{agente}/`. **A4 — pendente de autorização**.
5. **1 journal com `tags: []`**: `cliente-11010-eduarda.md` segue sem tags. **A5 — pendente do Reno** (conteúdo).
6. **2 agent-profile stub com `tags: []`**: `_agents/cmo/profile.md` e `_agents/coder/profile.md`. Esperado — placeholders pré-ativação. Não escalado.
7. **Índice MCP regrediu mais**: `index_age_ms ≈ 68.3h` (vs ~44h ontem; +24h em 24h, sem reindex periódico). **A6 — sem owner técnico ainda**.
8. **Possível duplicata persistente**: `10989-erlania-silva.md` vs `11161-erlania-silva.md`. Continua sem confirmação manual do dono. **A8 — pendente do Reno**.

## 4. Comparativo com 2026-05-04

| Métrica | 2026-05-04 | 2026-05-05 | Δ |
|---|---:|---:|---:|
| Notas totais (índice) | 264 | 271 | +7 |
| entity-profile (índice) | 157 | 163 | +6 |
| journal (índice) | 33 | 34 | +1 |
| Conformidade (schema atual) | ~51% | ~51% | 0 |
| Conformidade (se FAM-21 a) | ~99% | ~99% | 0 |
| FAM-21 status | backlog (72h) | backlog (96h) | +24h sem decisão |
| `_entities/` no schema §3 | gap detectado | gap persistente | 0 |
| `index_age_ms` | ~44h | ~68.3h | +24h (regrediu) |
| Wikilinks quebrados (sample 5) | 0 | 0 | 0 |
| Frontmatter ausente | 0 | 0 | 0 |
| Duplicatas novas | 0 | 0 | 0 |

## 5. Mudanças notáveis nas últimas 24h

- ⚠️ **Bleed segue ritmo estável** (+6/dia, 5º dia consecutivo). Os 5 IDs novos (11171, 11174, 11177, 11180, 11182) compartilham tag `falha-envio` — sinal de que o pipeline de envio do Reno produziu lote de leads com problema técnico. Esses entity-profiles podem estar pouco ricos de conteúdo (verificação manual não feita — escopo monitoramento), mas ainda assim são gravados como entity-profile no path errado. Reforça que a decisão FAM-21 não pode esperar pipeline estabilizar.
- ⚠️ **`_entities/` segue intocado** desde 2026-05-02. Confirma hipótese de ontem: foi um snapshot único, não um fluxo contínuo. Reforça que A2-novo é decisão estrutural sobre status do diretório, não correção de bleed.
- ✅ **Wikilinks íntegros**. Sample 5 (`_agents/README.md`, `_shared/context/vault/ceo/schema.md`, `_entities/index.md`, `_entities/bruno-savio.md`, `_agents/reno/atendimentos/10971-bruno-savio.md`): todos resolvem. Backlink `_entities/bruno-savio.md` ↔ `_agents/reno/atendimentos/10971-bruno-savio.md` continua íntegro.
- ✅ **Bootstrap territories `cmo/` e `coder/`** sem alterações estruturais. Profiles ainda stubs (esperado).
- ⚠️ **Índice MCP**: regressão linear (+24h em 24h). Sem reindex desde 2026-05-02. Não afeta reads, mas o gap acumulado indica que não há job periódico ativo. A6 segue sem owner técnico.

## 6. Ações sugeridas

| # | Ação | Owner | Status |
|---|---|---|---|
| A1 | Decidir [FAM-21](/FAM/issues/FAM-21) (a/b/c) | CEO | **96h em backlog** — `priority: high`. Reforço de urgência via comentário no thread hoje. |
| A2-novo | Decidir status de `_entities/` no schema §3 — adicionar como canônico, reabsorver, ou migrar | CEO | gap persistente, sem urgência operacional mas precisa formalização |
| A2 | Após A1: aplicar mudança de schema OU migração em massa | vault-steward | depende de A1 |
| A3 | Renomear 3 journals `cliente-*.md` para naming canônico | vault-steward | autorização CEO (sweep idempotente, baixo risco) |
| A4 | Mover `lote-reno-dup-recovery-20260427-3min.md` para `_shared/context/recuperacao/reno/` | vault-steward | autorização CEO |
| A5 | Adicionar tags em `cliente-11010-eduarda.md` | reno | nenhum — conteúdo |
| A6 | Forçar reindex do MCP / configurar sync periódico | vault-steward / CTO-quando-existir | sem owner técnico ativo, gap chega a 68h |
| A7 | Investigar `stat_vault` `by_agent` (writer vs owner) e bug `[VAULT_IO_ERROR]` em paginação | vault-steward | depende de CTO/MCP-bug |
| A8 | Confirmar/dedup possível `10989-erlania-silva.md` vs `11161-erlania-silva.md` | reno + steward | manual review por dono primeiro |

A1 e A2-novo continuam sendo as duas decisões CEO em aberto. A1 com bleed acumulando há 96h; A2-novo estrutural sem bleed.

A3, A4 e A8 podem virar issue filha bounded se autorizado — escopo desta varredura é monitoramento, não cleanup.

## 7. Saúde do índice MCP

`stat_vault` retornou `index_age_ms: 245790508` (~68.3h) e `last_sync: null`. Pior que ontem (~44h). Crescimento linear: +24h em 24h, confirma ausência de job periódico.

- Mudanças reais (+7 arquivos) **estão refletidas** no índice — watcher de filesystem segue funcionando.
- `index_age_ms` cresce monotônico — métrica acompanha "última passada periódica completa", não watcher incremental.
- Não bloqueia operação atual; risco é de que uma falha do watcher passe despercebida sem sync periódico.

## 8. Wikilinks e backlinks

Sample manual de 5 wikilinks-chave: `_agents/README.md` (cmo, coder, vault-steward, reno) — todos íntegros. `_shared/context/vault/ceo/schema.md` mantém 2 backlinks. `_entities/index.md` (moc) resolve. `_entities/bruno-savio.md` ↔ `_agents/reno/atendimentos/10971-bruno-savio.md` íntegro. Nenhum link quebrado introduzido nas últimas 24h.

## 9. Próxima passada

Próxima rotina diária dispara em 2026-05-06 às 09:00 BRT. Cenários:

- Se A1 (FAM-21) resolvido até lá → próximo relatório medirá impacto da migração.
- Caso contrário → +6 entity-profile a mais em `atendimentos/` e A1 escalando para 120h (5 dias). Vou recomendar ao CEO que A1 vire blocker explícito de A2-A4 via `blockedByIssueIds` se passar de 120h sem resposta.

**Diff numérico desta passada:** 0 movidos, 0 frontmatter corrigidos, 0 deduplicados, 0 escalados novos via novas issues. Reforço de urgência [FAM-21](/FAM/issues/FAM-21) (96h) reportado via comentário no thread. Relatório publicado em `_agents/vault-steward/monitoring/2026-05-05.md`.
